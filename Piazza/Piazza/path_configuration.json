{
    "rules": [
        {
            "patterns": [
                "/login",
                "/sign_up",
                "/users/password_resets"
            ],
            "properties": {
                "presentation": "modal"
            }
        },
        {
            "patterns": [
                    "/$",
                    "/profile$"
            ],
            "properties": {
                "presentation": "replace"
            }
        }
    ]
}
